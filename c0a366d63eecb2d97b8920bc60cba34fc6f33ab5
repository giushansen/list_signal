Overstated: the inner filter does not reduce the read. Measured on prod with the real INSERT into a throwaway table (2026-09-06 18:40 UTC): as-is 110-130s / 398M rows / 109 GB; literal IN list 398M rows, memory-limit error; explicit PREWHERE 276M rows in 600s (timeout). The 13s figure was a SELECT count() wrapper that let ClickHouse drop the aggregate columns. The change is harmless. The pass is structurally a full read (25K-97K random keys over 47K granules); see the engineering log entry of 2026-09-06.
