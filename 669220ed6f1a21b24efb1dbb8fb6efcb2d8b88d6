Deployed to the master 2026-09-09 15:48 UTC (release 0.1.0-202609092344). Verified: ss shows epmd on 10.0.0.1:4369 plus loopback and dist on 10.0.0.1:9100 only; CSP header present on every page; ls_app is the only user the app queries as (query_log). One surprise: PrivateTmp=yes in the new hardening drop-in hid the legacy /tmp snapshot from the process, so this restart started with cold caches once (the warm-up covered the heavy pages). Correction to the message above: Clickhouse.escape/1 already escaped quotes and backslashes; it only stripped semicolons, so the interpolation risk was smaller than stated.
